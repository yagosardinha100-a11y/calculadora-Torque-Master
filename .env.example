# URL pública do app (opcional; usada pelo server local)
APP_URL=

# Firebase (client) — configure no console Firebase e injete via build env se necessário.
# O app atual lê a config de src/lib/firebase.ts.
# VITE_FIREBASE_API_KEY=
# VITE_FIREBASE_AUTH_DOMAIN=
# VITE_FIREBASE_PROJECT_ID=
